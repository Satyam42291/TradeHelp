streamlit==1.34.0
pandas==2.2.3
numpy==1.26.4
plotly==5.22.0

# yfinance — use a stable version that avoids "No timezone found" + JSON parse errors
yfinance==0.2.25

# forecasting
prophet==1.1.5

# dependency for Prophet (makes installation work properly)
cmdstanpy>=1.1.0

# plotting
matplotlib==3.8.4

# required for direct Yahoo CSV fallback
requests==2.31.0
